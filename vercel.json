{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm --prefix backend run prisma:deploy && npm --prefix backend run prisma:generate && npm --prefix backend run build",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
