{
  "version": 2,
  "buildCommand": "cd backend && npm run build && npm run prisma:generate",
  "builds": [
    {
      "src": "backend/package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/dist/server.js"
    }
  ]
}